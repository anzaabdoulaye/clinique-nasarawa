<form method="post" action="{{ path('app_honoraire_medecin_delete', {'id': honoraire_medecin.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ honoraire_medecin.id) }}">
    <button class="btn">Delete</button>
</form>
