<!-- ========== App Menu ========== -->
<style>

/* =============================
   LOGO CLINIQUE
============================= */

.navbar-brand-box{
    height:auto !important;
    padding: 5px;
    text-align:center;
}

/* logo circulaire */
.clinique-logo{
    width:90px;
    height:90px;
    border-radius:50%;
    object-fit:cover;
    background:white;
    padding:3px;
    box-shadow:0 4px 10px rgba(0,0,0,0.15);
    transition:all 0.3s;
}

/* effet hover */
.clinique-logo:hover{
    transform:scale(1.05);
}

/* logo réduit sidebar */
.logo-sm .clinique-logo{
    width:40px;
    height:40px;
}

/* alignement logo */
.logo{
    display:flex;
    align-items:center;
    justify-content:center;
}

/* espace menu */
.menu-title{
    margin-top:10px;
}

</style>

<div class="app-menu navbar-menu">

    <!-- LOGO -->
    <div class="navbar-brand-box">

        <a href="{{ path('home') }}" class="logo logo-light">

            <span class="logo-lg">
                <img src="{{ asset('logo.jpeg') }}" 
                     alt="Clinique Nassarawa"
                     class="clinique-logo">
            </span>

            <span class="logo-sm">
                <img src="{{ asset('logo.jpeg') }}" 
                     alt="Clinique Nassarawa"
                     class="clinique-logo">
            </span>

        </a>

        <button type="button"
                class="btn btn-sm p-0 fs-20 header-item float-end btn-vertical-sm-hover"
                id="vertical-hover">
            <i class="ri-record-circle-line"></i>
        </button>

    </div>

    <!-- USER -->
<div class="dropdown sidebar-user m-2 rounded">
    <button type="button" class="btn material-shadow-none" data-bs-toggle="dropdown">
        <span class="d-flex align-items-center gap-2">
            <img class="rounded header-profile-user"
                 src="{{ asset('assets/images/users/avatar-1.jpg') }}"
                 alt="Header Avatar">

            <span class="text-start">
                <span class="d-block fs-14 sidebar-user-name-sub-text">
                    <i class="ri ri-circle-fill fs-10 text-success align-baseline"></i>
                    <span class="align-middle">Online</span>
                </span>
            </span>
        </span>
    </button>
</div>

<div id="scrollbar">
    <div class="container-fluid">
        <ul class="navbar-nav">

            <li class="menu-title">
                <span>MENU</span>
            </li>

            <li class="nav-item">
                <a class="nav-link menu-link {{ app.request.attributes.get('_route') == 'home' ? 'active' : '' }}"
                   href="{{ path('home') }}">
                    <i class="ri-dashboard-line"></i>
                    <span>Tableau de bord</span>
                </a>
            </li>

            {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_ACCUEIL') or is_granted('ROLE_MEDECIN') or is_granted('ROLE_INFIRMIER') %}
                <li class="nav-item">
                    <a class="nav-link menu-link {{ app.request.attributes.get('_route') starts with 'app_patient_' ? 'active' : '' }}"
                       href="{{ path('app_patient_index') }}">
                        <i class="ri-group-line"></i>
                        <span>Patients</span>
                    </a>
                </li>
            {% endif %}

            {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_ACCUEIL') or is_granted('ROLE_MEDECIN') or is_granted('ROLE_INFIRMIER') %}
                <li class="nav-item">
                    <a class="nav-link menu-link {{ app.request.attributes.get('_route') starts with 'app_consultation_' ? 'active' : '' }}"
                       href="{{ path('app_consultation_index') }}">
                        <i class="ri-health-book-line"></i>
                        <span>Consultations</span>
                    </a>
                </li>
            {% endif %}

            {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_LABO') or is_granted('ROLE_MEDECIN') %}
                <li class="nav-item">
                    <a class="nav-link menu-link {{ app.request.attributes.get('_route') starts with 'app_laboratoire_' ? 'active' : '' }}"
                       href="{{ path('app_laboratoire_index') }}">
                        <i class="ri-flask-line"></i>
                        <span>Laboratoire</span>
                    </a>
                </li>
            {% endif %}

            {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_ACCUEIL') or is_granted('ROLE_MEDECIN') %}
                <li class="nav-item">
                    <a class="nav-link menu-link {{ app.request.attributes.get('_route') starts with 'app_rendez_vous_' ? 'active' : '' }}"
                       href="{{ path('app_rendez_vous_index') }}">
                        <i class="ri-calendar-event-line"></i>
                        <span>Rendez-vous</span>
                    </a>
                </li>
            {% endif %}

            {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_ACCUEIL') or is_granted('ROLE_PERCEPTION') %}
                <li class="nav-item">
                    <a class="nav-link menu-link {{ app.request.attributes.get('_route') starts with 'app_facture_' ? 'active' : '' }}"
                       href="{{ path('app_facture_index') }}">
                        <i class="ri-cash-line"></i>
                        <span>Factures</span>
                    </a>
                </li>
            {% endif %}

            {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_PERCEPTION') %}
                <li class="nav-item">
                    <a class="nav-link menu-link {{ app.request.attributes.get('_route') starts with 'app_perception_' ? 'active' : '' }}"
                       href="{{ path('app_perception_index') }}">
                        <i class="ri-folder-chart-line"></i>
                        <span>Perception</span>
                    </a>
                </li>
            {% endif %}

            {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_HOSPITALISATION') or is_granted('ROLE_MEDECIN') or is_granted('ROLE_INFIRMIER') %}
                <li class="nav-item">
                    <a class="nav-link menu-link {{ app.request.attributes.get('_route') starts with 'app_hospitalisation_' ? 'active' : '' }}"
                       href="{{ path('app_hospitalisation_index') }}">
                        <i class="ri-hospital-line"></i>
                        <span>Hospitalisation</span>
                    </a>
                </li>
            {% endif %}

            {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_PHARMACIE') %}
                <li class="nav-item">
                    <a class="nav-link menu-link {{ app.request.attributes.get('_route') starts with 'app_pharmacie_' or app.request.attributes.get('_route') starts with 'app_medicament_' or app.request.attributes.get('_route') starts with 'app_lot_' ? 'active' : '' }}"
                       href="{{ path('app_pharmacie_index') }}">
                        <i class="ri-capsule-line"></i>
                        <span>Pharmacie</span>
                    </a>
                </li>
            {% endif %}

            {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_COMPTA_MATIERE') %}
                <li class="nav-item">
                    <a class="nav-link menu-link {{ app.request.attributes.get('_route') starts with 'app_comptabilite_matiere_' ? 'active' : '' }}"
                       href="{{ path('app_comptabilite_matiere_index') }}">
                        <i class="ri-archive-stack-line"></i>
                        <span>Comptabilité Matières</span>
                    </a>
                </li>
            {% endif %}

            <li class="menu-title">
                <span>AUTRES</span>
            </li>

            {% if is_granted('ROLE_ADMIN') %}
                <li class="nav-item">
                    <a class="nav-link menu-link {{ app.request.attributes.get('_route') starts with 'app_utilisateur_' ? 'active' : '' }}"
                       href="{{ path('app_utilisateur_index') }}">
                        <i class="ri-user-line"></i>
                        <span>Utilisateurs</span>
                    </a>
                </li>

                <li class="nav-item">
                    <a class="nav-link menu-link {{ app.request.attributes.get('_route') == 'app_utilisateur_param' ? 'active' : '' }}"
                       href="{{ path('app_utilisateur_param') }}">
                        <i class="ri-settings-5-line"></i>
                        <span>Paramètres</span>
                    </a>
                </li>
            {% endif %}

        </ul>
    </div>
</div>

    <div class="sidebar-background"></div>

</div>