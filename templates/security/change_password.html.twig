<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Sécurité — Modifier le mot de passe | Clinique Nasarawa</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body{
            background:
                radial-gradient(900px 450px at 10% 10%, rgba(13,110,253,.10), transparent 55%),
                radial-gradient(700px 400px at 90% 15%, rgba(25,135,84,.08), transparent 55%),
                #f6f8fb;
        }

        .auth-shell{
            min-height: 100vh;
            display:flex;
            align-items:center;
        }

        .auth-card{
            border: 1px solid rgba(0,0,0,.06);
            box-shadow: 0 18px 45px rgba(15,23,42,.10);
        }

        .brand-badge{
            width: 54px;
            height: 54px;
            border-radius: 18px;
            display:grid;
            place-items:center;
            background: rgba(13,110,253,.10);
            border: 1px solid rgba(13,110,253,.15);
        }

        .divider{
            height:1px;
            background: rgba(0,0,0,.06);
        }

        .help-text{
            font-size: .85rem;
            color:#6c757d;
        }

        .form-control:focus{
            box-shadow: 0 0 0 .25rem rgba(13,110,253,.15);
            border-color: rgba(13,110,253,.45);
        }

        .btn-primary{
            box-shadow: 0 12px 24px rgba(13,110,253,.20);
        }
    </style>
</head>

<body>

<div class="container auth-shell">
    <div class="row w-100 justify-content-center">
        <div class="col-12 col-md-9 col-lg-6 col-xl-5">

            <div class="card auth-card rounded-4 p-4 p-md-5">

                <!-- Header -->
                <div class="d-flex align-items-start gap-3 mb-4">
                    <div class="brand-badge">
                        <svg width="26" height="26" viewBox="0 0 24 24" fill="none">
                            <path d="M16 10V8a4 4 0 0 0-8 0v2"
                                  stroke="#0d6efd" stroke-width="1.8" stroke-linecap="round"/>
                            <path d="M7 10h10a2 2 0 0 1 2 2v7a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2v-7a2 2 0 0 1 2-2Z"
                                  stroke="#0d6efd" stroke-width="1.6"/>
                            <path d="M12 14v3"
                                  stroke="#0d6efd" stroke-width="1.8" stroke-linecap="round"/>
                        </svg>
                    </div>

                    <div>
                        <h1 class="h5 fw-bold mb-1">Sécurité du compte</h1>
                        <div class="text-muted small">
                            Clinique Nasarawa — Mise à jour du mot de passe
                        </div>
                    </div>
                </div>

                <!-- Conseils -->
                <div class="alert alert-info rounded-3 mb-4">
                    <div class="fw-semibold mb-1">Recommandations</div>
                    <ul class="mb-0 small ps-3">
                        <li>Minimum 8 caractères</li>
                        <li>Au moins 1 majuscule, 1 chiffre et 1 symbole</li>
                        <li>Ne pas utiliser vos informations personnelles</li>
                    </ul>
                </div>

                {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}

                    {% if form.vars.errors|length > 0 %}
                        <div class="alert alert-danger rounded-3">
                            {{ form_errors(form) }}
                        </div>
                    {% endif %}

                    <div class="mb-3">
                        {{ form_row(form.plainPassword.first, {
                            'label': 'Nouveau mot de passe',
                            'attr': {
                                'class': 'form-control rounded-3',
                                'autocomplete': 'new-password',
                                'placeholder': '••••••••'
                            }
                        }) }}
                        <div class="help-text mt-1">
                            Utilisez un mot de passe unique.
                        </div>
                    </div>

                    <div class="mb-4">
                        {{ form_row(form.plainPassword.second, {
                            'label': 'Confirmation',
                            'attr': {
                                'class': 'form-control rounded-3',
                                'autocomplete': 'new-password',
                                'placeholder': '••••••••'
                            }
                        }) }}
                        <div class="help-text mt-1">
                            Saisissez à nouveau le même mot de passe.
                        </div>
                    </div>

                    <div class="divider my-4"></div>

                    <div class="d-flex justify-content-between gap-2">
                        <a href="{{ path('home') }}"
                           class="btn btn-outline-secondary rounded-3">
                            Annuler
                        </a>

                        <button type="submit"
                                class="btn btn-primary rounded-3 fw-semibold px-4">
                            Mettre à jour
                        </button>
                    </div>

                {{ form_end(form) }}

                <div class="text-center mt-4">
                    <small class="text-muted">
                        © {{ "now"|date("Y") }} Clinique Nasarawa
                    </small>
                </div>

            </div>

            <div class="text-center mt-3">
                <small class="text-muted">
                    Besoin d’aide ? Contactez l’administrateur système.
                </small>
            </div>

        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>