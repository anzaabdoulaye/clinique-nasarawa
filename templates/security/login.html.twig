<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Connexion | Clinique Nasarawa</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    {# Bootstrap 5 CDN #}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        :root{
            --cn-primary:#0d6efd;
            --cn-muted:#6c757d;
            --cn-card:#ffffff;
        }
        body{
            background:
                radial-gradient(1200px 600px at 10% 10%, rgba(13,110,253,.12), transparent 55%),
                radial-gradient(900px 500px at 90% 20%, rgba(25,135,84,.10), transparent 50%),
                #f6f8fb;
        }
        .auth-shell{
            min-height: 100vh;
        }
        .auth-card{
            background: var(--cn-card);
            border: 1px solid rgba(0,0,0,.06);
            box-shadow: 0 18px 45px rgba(15, 23, 42, .10);
        }
        .brand-badge{
            width: 56px;
            height: 56px;
            border-radius: 16px;
            display: grid;
            place-items: center;
            background: rgba(13,110,253,.10);
            border: 1px solid rgba(13,110,253,.15);
        }
        .form-control:focus{
            box-shadow: 0 0 0 .25rem rgba(13,110,253,.15);
            border-color: rgba(13,110,253,.45);
        }
        .btn-primary{
            box-shadow: 0 12px 24px rgba(13,110,253,.20);
        }
        .divider{
            height: 1px;
            background: rgba(0,0,0,.06);
        }
        .small-link{
            text-decoration: none;
        }
        .small-link:hover{
            text-decoration: underline;
        }
    </style>
</head>

<body>

<div class="container auth-shell d-flex align-items-center">
    <div class="row w-100 justify-content-center">
        <div class="col-12 col-sm-10 col-md-7 col-lg-5 col-xl-4">

            <div class="card auth-card rounded-4 p-4 p-md-5">

                {# Header / Branding #}
                <div class="d-flex align-items-center gap-3 mb-4">
                    <div class="brand-badge">
                        {# Logo simple style "clinique" (remplaçable) #}
                        <svg width="28" height="28" viewBox="0 0 24 24" fill="none" aria-hidden="true">
                            <path d="M12 2C7.58 2 4 5.58 4 10c0 5.25 6.36 11.54 7.33 12.49.38.37.98.37 1.36 0C13.64 21.54 20 15.25 20 10c0-4.42-3.58-8-8-8Z" stroke="#0d6efd" stroke-width="1.6"/>
                            <path d="M12 7v6M9 10h6" stroke="#0d6efd" stroke-width="1.8" stroke-linecap="round"/>
                        </svg>
                    </div>

                    <div class="flex-grow-1">
                        <h1 class="h4 fw-bold mb-1">Clinique Nasarawa</h1>
                        <div class="text-muted small">Accès sécurisé au portail</div>
                    </div>
                </div>

                {# Message d'erreur #}
                {% if error %}
                    <div class="alert alert-danger d-flex align-items-start gap-2 rounded-3" role="alert">
                        <div class="mt-1">
                            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" aria-hidden="true">
                                <path d="M12 9v4" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
                                <path d="M12 17h.01" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
                                <path d="M10.29 3.86h3.42c.67 0 1.29.35 1.64.93l6.2 10.33c.72 1.2-.14 2.72-1.54 2.72H3.99c-1.4 0-2.26-1.52-1.54-2.72l6.2-10.33c.35-.58.97-.93 1.64-.93Z"
                                      stroke="currentColor" stroke-width="1.6"/>
                            </svg>
                        </div>
                        <div class="flex-grow-1">
                            <div class="fw-semibold">Connexion impossible</div>
                            <div class="small">
                                {{ error.messageKey|trans(error.messageData, 'security') }}
                            </div>
                        </div>
                    </div>
                {% endif %}

                <form method="post" class="mt-2" novalidate>

                    <div class="form-floating mb-3">
                        <input type="text"
                               value="{{ last_username }}"
                               name="_username"
                               id="username"
                               class="form-control rounded-3"
                               placeholder="Nom d'utilisateur"
                               autocomplete="username"
                               required autofocus>
                        <label for="username">Nom d'utilisateur</label>
                    </div>

                    <div class="form-floating mb-2">
                        <input type="password"
                               name="_password"
                               id="password"
                               class="form-control rounded-3"
                               placeholder="Mot de passe"
                               autocomplete="current-password"
                               required>
                        <label for="password">Mot de passe</label>
                    </div>

                    <div class="d-flex justify-content-between align-items-center mb-3">
                        <div class="form-check">
                            <input class="form-check-input" type="checkbox" value="1" id="remember_me" name="_remember_me">
                            <label class="form-check-label small text-muted" for="remember_me">
                                Se souvenir de moi
                            </label>
                        </div>

                        {# Mets la route que tu veux (ex: app_forgot_password_request si tu utilises reset-password-bundle) #}
                        <a href="#"
                           class="small text-primary small-link">
                            Mot de passe oublié ?
                        </a>
                    </div>

                    <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

                    <button class="btn btn-primary w-100 py-2 rounded-3 fw-semibold" type="submit">
                        Se connecter
                    </button>

                </form>

                <div class="my-4 divider"></div>

                {# Footer #}
                <div class="d-flex justify-content-between align-items-center">
                    <small class="text-muted">© {{ "now"|date("Y") }} Clinique Nasarawa</small>
                    <small class="text-muted">
                        Assistance : <span class="fw-semibold">Service IT</span>
                    </small>
                </div>

            </div>

            <div class="text-center mt-3">
                <small class="text-muted">
                    Vos informations sont protégées et chiffrées.
                </small>
            </div>

        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
{# 
{% block title %}Log in!{% endblock %}

{% block body %}
    <form method="post">
        {% if error %}
            <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
        {% endif %}

        {% if app.user %}
            <div class="mb-3">
                You are logged in as {{ app.user.userIdentifier }}, <a href="{{ logout_path() }}">Logout</a>
            </div>
        {% endif %}

        <h1 class="h3 mb-3 font-weight-normal">Please sign in</h1>
        <label for="username">Username</label>
        <input type="text" value="{{ last_username }}" name="_username" id="username" class="form-control" autocomplete="username" required autofocus>
        <label for="password">Password</label>
        <input type="password" name="_password" id="password" class="form-control" autocomplete="current-password" required>
        <input type="hidden" name="_csrf_token" data-controller="csrf-protection" value="{{ csrf_token('authenticate') }}">

        {#
            Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
            See https://symfony.com/doc/current/security/remember_me.html

            <div class="checkbox mb-3">
                <input type="checkbox" name="_remember_me" id="_remember_me">
                <label for="_remember_me">Remember me</label>
            </div>
        #}
{# 
        <button class="btn btn-lg btn-primary" type="submit">
            Sign in
        </button>
    </form>
{% endblock %} #} 
