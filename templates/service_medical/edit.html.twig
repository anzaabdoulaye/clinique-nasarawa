{# templates/service_medical/edit.html.twig
   ⚠️ Ce template est injecté dans le modal via fetch => PAS de extends base ici.
#}

{{ form_start(form, {
  'action': path('app_service_medical_edit', { id: service_medical.id }),
  'attr': { 'id': 'ServiceMedicalEditForm' }
}) }}

<div class="row g-3">
  <div class="col-md-12">
    {{ form_row(form.libelle) }}
  </div>
</div>

<div class="d-flex justify-content-end mt-4 gap-2">
  <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">
    Annuler
  </button>
  <button type="submit" class="btn btn-primary">
    Mettre à jour
  </button>
</div>

{{ form_end(form) }}